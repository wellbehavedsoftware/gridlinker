---

- name: "reload upstart config"
  shell: "initctl reload-configuration"
  sudo: "yes"

- name: "set etcd.needs_restart to 'yes'"
  update_resource:
    etcd.needs_restart: "yes"
  changed_when: "True"
  notify: "restart etcd"

- name: "restart etcd"
  action:
    module: "service"
    name: "etcd"
    state: "restarted"
  sudo: "yes"
  notify: "set etcd.needs_restart to 'no'"

- name: "set etcd.needs_restart to 'no'"
  update_resource:
    etcd.needs_restart: "no"
  changed_when: "True"

# ex: et ts=2 filetype=yaml
