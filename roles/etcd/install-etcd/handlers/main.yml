---

- name: "reload upstart config"
  shell: "initctl reload-configuration"
  sudo: "yes"

- name: "restart etcd"
  action:
    module: "service"
    name: "etcd"
    state: "{{ 'restarted' if etcd_state == 'running' else 'stopped' }}"
  sudo: "yes"

# ex: et ts=2 filetype=yaml
