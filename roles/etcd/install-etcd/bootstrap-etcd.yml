---

- hosts: "host"
  max_fail_percentage: "0"
  tasks:

    - set_fact:
        etcd_state: "running"
      when: "etcd_state == 'configured'"

- hosts: "host"
  roles: [ "install-etcd" ]

# ex: et ts=2 filetype=yaml
