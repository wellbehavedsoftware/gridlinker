---

- name: "set php.state to 'configuring'"
  set_fact_dynamic:
    php.state: "configuring"
  when: "php_state == 'unconfigured'"
  tags: [ "php", "setup-php" ]

- include: "setup-php.yml"
  tags: [ "php", "setup-php" ]

- name: "set php.state to 'unsynchronized'"
  set_fact_dynamic:
    php.state: "stopped"
  when: "php_state == 'configuring'"
  tags: [ "php", "setup-php" ]

# ex: et ts=2 filetype=yaml
