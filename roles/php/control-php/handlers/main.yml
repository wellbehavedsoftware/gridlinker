---

- name: "reload upstart config"
  shell: "initctl reload-configuration"
  sudo: "yes"

- name: "set php.needs_restart to 'yes'"
  set_fact_dynamic:
    php.needs_restart: "yes"
  changed_when: "True"
  notify: "start, restart or stop php"

- name: "start, restart or stop php"
  action:
    module: "service"
    name: "php5-fpm"
    state: "{{
      'restarted' if nginx_needs_restart == 'yes'
      else 'started' if nginx_state in [ 'starting', 'running' ]
      else 'stopped'
    }}"
  sudo: "yes"
  changed_when: "True"
  notify: "set php.needs_restart to 'no'"

- name: "set php.needs_restart to 'no'"
  set_fact_dynamic:
    php.needs_restart: "no"
  changed_when: "True"

# ex: et ts=2 filetype=yaml
