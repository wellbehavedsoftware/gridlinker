---

- include: "postgresql-control.yml"
  when: "
    postgresql_install == 'yes'
    and postgresql_state in [ 'enabled', 'disabled' ]
  "
  tags:

    - "postgresql-control"
    - "postgresql"

# ex: et ts=2 filetype=yaml
