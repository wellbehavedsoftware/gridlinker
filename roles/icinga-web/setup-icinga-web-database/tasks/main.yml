---

- include: "setup-icinga-web-database-mysql.yml"
  when: "icinga_web_main_database_type == 'mysql'"
  tags:

    - "icinga-web"
    - "setup-icinga-web-database"

- include: "setup-icinga-web-database-postgresql.yml"
  when: "icinga_web_main_database_type == 'postgresql'"
  tags:

    - "icinga-web"
    - "setup-icinga-web-database"

# ex: et ts=2 filetype=yaml
