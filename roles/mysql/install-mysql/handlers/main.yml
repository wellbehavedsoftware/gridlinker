---

- name: "reload upstart config"
  shell: "initctl reload-configuration"
  sudo: "yes"

- name: "restart mysql"
  action:
    module: "service"
    name: "mysql"
    state: "{{ 'restarted' if mysql_state == 'running' else 'stopped' }}"
  sudo: "yes"

# ex: et ts=2 filetype=yaml
