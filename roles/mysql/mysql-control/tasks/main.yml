---

- include: "mysql-control.yml"
  when: "
    mysql_install == 'yes'
    and mysql_state in [ 'enabled', 'disabled' ]
  "
  tags:

    - "mysql"
    - "mysql-control"

# ex: et ts=2 filetype=yaml
