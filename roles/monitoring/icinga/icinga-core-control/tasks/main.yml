---

- include: "icinga-core-control.yml"
  when: "icinga_core_state in [ 'enabled', 'disabled' ]"
  tags:

    - "icinga-core-control"
    - "icinga-core"
    - "icinga"

# ex: et ts=2 filetype=yaml
