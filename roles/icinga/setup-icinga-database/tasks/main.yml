---

- include: "setup-icinga-database-mysql.yml"
  when: "icinga_web_main_database_type == 'mysql'"
  tags:

    - "icinga"
    - "setup-icinga-database"

- include: "setup-icinga-database-postgresql.yml"
  when: "icinga_web_main_database_type == 'postgresql'"
  tags:

    - "icinga"
    - "setup-icinga-database"

# ex: et ts=2 filetype=yaml
