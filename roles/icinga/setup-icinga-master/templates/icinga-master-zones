{% for host_name in icinga_master_hosts %}
{% set host = hostvars [host_name] %}

object Endpoint "{{ host_name }}" {
	host = "{{ host_name }}"
}

{% endfor %}

object Zone "master" {
	endpoints = [ "{{ icinga_master_hosts | join ('", "') }}" ]
}
