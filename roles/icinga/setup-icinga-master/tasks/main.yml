---

- update_resource:
    icinga.state: "configuring"
  when: "icinga_state == 'unconfigured'"

- include: "setup-icinga-master.yml"
  tags: "setup-icinga-master"

- update_resource:
    icinga.state: "stopped"
  when: "icinga_state == 'configuring'"

# ex: et ts=2 filetype=yaml
