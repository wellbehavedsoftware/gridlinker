---

- name: "check private address"
  when: "private_address | default ('') == ''"
  fail:

    msg: "must set 'private_address'"

- name: "configure hosts"
  become: "yes"
  action:

    module: "template"
    dest: "/etc/hosts"
    src: "setup-hosts-file"

# ex: et ts=2 filetype=yaml
